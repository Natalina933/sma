// Authentification
export const loginSuccess = () => {
    // Simule une connexion réussie
  };
  
  export const loginFailure = () => {
    // Simule une erreur de connexion
  };
  
  export const logout = () => {
    // Simule une déconnexion
  };
  
  export const resetPassword = () => {
    // Simule la réinitialisation du mot de passe
  };
  